import Foundation

/// Dummy class purely for separation of concerns, that shows this can be the ground zero for `SwiftUI` configurations,
/// that are depending on old logics.
final class SwiftUIAppDelegate: AppDelegateType {
    // no-op
}
