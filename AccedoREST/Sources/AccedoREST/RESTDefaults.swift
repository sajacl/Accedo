import Foundation

extension REST {
    /// The language provided to all network requests, defaults to `en`.
    static let language = "en"

    /// API host.
    static let apiHost = "api.themoviedb.org"

    /// Default network timeout for API requests.
    static let defaultAPINetworkTimeout: TimeInterval = 30.0
}
